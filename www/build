#!/bin/bash

# Install bun
curl -fsSL https://bun.sh/install | bash

# Bundle TypeScript code
bun build --minify --outdir=production ./index.ts

# If you want to serve the files during development
# bun x serve .

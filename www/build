#!/bin/bash

# Install bun
curl -fsSL https://bun.sh/install | bash

mkdir -p ./production

# Bundle TypeScript code
bun build --target=browser --minify --outdir=production ./index.ts

mv ../wasm_bg.wasm ./production/

mv ./index.html ./production/

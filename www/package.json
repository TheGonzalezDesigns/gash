{
  "name": "www",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "make": "bun build --minify --target=browser --outdir=./production ./index.ts > compile.log 2>&1",
    "test_unit": "bun test ./test.ts > ./tests/runtime.log 2>&",
    "record_build": "bun build --target=browser ./_index.ts > ./tests/build.log 2>&"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1.7.0"
  }
}
